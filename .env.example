# Fase 1 no requiere variables de entorno.
#
# Reglas futuras:
# - Las claves privadas de Supabase, Resend u otros servicios no deben usar prefijo PUBLIC_.
# - Las variables PUBLIC_ solo deben contener datos deliberadamente públicos.
# - Los precios mayoristas y datos sensibles deben salir de queries server-side protegidas.
